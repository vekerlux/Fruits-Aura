{
    "buildCommand": "cd app/client && npm install --legacy-peer-deps && npm run build",
    "outputDirectory": "app/client/dist",
    "installCommand": "echo skip",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}